<?xml version="1.0"?>
<ruleset name="WordPress Starter Theme">
    <description>WordPress Starter Theme Coding Standards - PSR-4 Compatible</description>

    <!-- Scan these files -->
    <file>./src</file>
    <file>./config</file>
    <file>./tests</file>
    <file>./functions.php</file>
    <file>./index.php</file>
    <file>./bin</file>
    <file>./scripts</file>

    <!-- Ignore vendor, compiled files, and Blade templates. Blade syntax cannot be parsed
         by phpcs, so templates/ gets no WordPress.Security escaping sniffs from this
         ruleset at all; escaping in templates/ is covered instead by
         tests/Unit/TemplateRenderTest.php (hostile-payload render pass) and the @kses
         Blade directive, not by phpcs. -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/compiled/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*.blade.php</exclude-pattern>

    <!-- ./scripts also holds build-tooling JS (sync-icons.js, transform-tokens.js) that this
         PHP ruleset was never meant to cover; only the PHP CLI scripts in that folder are in scope. -->
    <exclude-pattern>*/scripts/*.js</exclude-pattern>

    <!-- PHP version -->
    <config name="minimum_supported_wp_version" value="6.8"/>
    <config name="testVersion" value="8.2-"/>

    <!-- Use WordPress coding standards with PSR-4/PSR-12 exclusions -->
    <rule ref="WordPress">
        <!-- Allow short array syntax (modern PHP) -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>

        <!-- Allow short ternary (null coalescing) -->
        <exclude name="Universal.Operators.DisallowShortTernary"/>

        <!-- PSR-4: Allow PascalCase class names and filenames -->
        <exclude name="WordPress.Files.FileName"/>

        <!-- PSR-4: Allow camelCase method names -->
        <exclude name="WordPress.NamingConventions.ValidFunctionName"/>

        <!-- PSR-4: Allow camelCase variables and properties -->
        <exclude name="WordPress.NamingConventions.ValidVariableName"/>

        <!-- PSR-12: Allow spaces for indentation -->
        <exclude name="Generic.WhiteSpace.DisallowSpaceIndent"/>

        <!-- PSR-12: Allow opening brace on new line -->
        <exclude name="Generic.Functions.OpeningFunctionBraceKernighanRitchie"/>
        <exclude name="Generic.Classes.OpeningBraceSameLine"/>
        <exclude name="WordPress.WhiteSpace.ControlStructureSpacing.OpenBraceNotSameLine"/>

        <!-- Allow Yoda conditions to be optional -->
        <exclude name="WordPress.PHP.YodaConditions"/>

        <!-- Allow inline comments without punctuation -->
        <exclude name="Squiz.Commenting.InlineComment.InvalidEndChar"/>

        <!-- Allow missing doc comments (use PHPStan for type checking) -->
        <exclude name="Squiz.Commenting.FileComment"/>
        <exclude name="Squiz.Commenting.ClassComment"/>
        <exclude name="Squiz.Commenting.FunctionComment"/>
        <exclude name="Squiz.Commenting.VariableComment"/>
        <exclude name="Generic.Commenting.DocComment.MissingShort"/>

        <!-- PSR-12: Allow no spaces inside parentheses -->
        <exclude name="WordPress.WhiteSpace.ControlStructureSpacing"/>
        <exclude name="PEAR.Functions.FunctionCallSignature"/>
        <exclude name="Squiz.Functions.FunctionDeclarationArgumentSpacing"/>
        <exclude name="Generic.Functions.FunctionCallArgumentSpacing"/>

        <!-- PSR-12: Allow no spaces in arrays -->
        <exclude name="WordPress.Arrays.ArrayDeclarationSpacing"/>
        <exclude name="WordPress.Arrays.ArrayKeySpacingRestrictions"/>
        <exclude name="NormalizedArrays.Arrays.ArrayBraceSpacing"/>
        <exclude name="WordPress.Arrays.MultipleStatementAlignment"/>

        <!-- Allow PSR-12 operator spacing -->
        <exclude name="WordPress.WhiteSpace.OperatorSpacing"/>
        <exclude name="Squiz.WhiteSpace.LogicalOperatorSpacing"/>

        <!-- Allow PSR-12 equals alignment (no alignment) -->
        <exclude name="Generic.Formatting.MultipleStatementAlignment"/>

        <!-- PSR-1: Classes must be in a namespace -->
        <exclude name="PSR1.Classes.ClassDeclaration.MissingNamespace"/>

        <!-- Vite handles versioning -->
        <exclude name="WordPress.WP.EnqueuedResourceParameters"/>

        <!-- Allow direct filesystem operations in theme code. Narrowed to the
             filesystem/file_get_contents codes so the curl group (unused in this
             codebase) and other AlternativeFunctions codes (rand, parse_url,
             json_encode, strip_tags) stay active. -->
        <exclude name="WordPress.WP.AlternativeFunctions.file_get_contents_file_get_contents"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_chmod"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fclose"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_file_put_contents"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fopen"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fread"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_fsockopen"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_mkdir"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_readfile"/>
        <exclude name="WordPress.WP.AlternativeFunctions.file_system_operations_rmdir"/>
        <exclude name="WordPress.WP.AlternativeFunctions.rename_rename"/>
        <exclude name="WordPress.WP.AlternativeFunctions.unlink_unlink"/>

        <!-- Allow error suppression for fsockopen -->
        <exclude name="WordPress.PHP.NoSilencedErrors"/>

        <!-- Allow trailing whitespace (handled by editor) -->
        <exclude name="Squiz.WhiteSpace.SuperfluousWhitespace"/>

        <!-- Allow common parameter names like $default, $class -->
        <exclude name="Universal.NamingConventions.NoReservedKeywordParameterNames"/>

        <!-- Allow unused method parameters (often required by interfaces/hooks) -->
        <exclude name="Generic.CodeAnalysis.UnusedFunctionParameter"/>

        <!-- Allow error_log() (see src/Providers/LogServiceProvider.php,
             config/index.php); other DevelopmentFunctions codes (var_dump, print_r,
             debug_backtrace, path-disclosure error_reporting) stay active. -->
        <exclude name="WordPress.PHP.DevelopmentFunctions.error_log_error_log"/>

    </rule>

    <!-- Require newline at end of file -->
    <rule ref="Generic.Files.EndFileNewline"/>

    <!-- WordPress security checks (important to keep!) -->
    <rule ref="WordPress.Security"/>
    <rule ref="WordPress.DB"/>
</ruleset>